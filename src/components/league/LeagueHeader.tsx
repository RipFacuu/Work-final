const LeagueHeader: React.FC<Props> = ({ league }) => {
  return (
    <div>
      <h1>{league.name}</h1> {/* Esto debería mostrar "Liga Participando" */}
      // ... existing code ...
    </div>
  );
};